# Automation tests (Cypress scaffold)

This folder contains a minimal scaffold for Cypress end-to-end tests. To run locally:

```bash
npm init -y
npm install cypress --save-dev
npx cypress open
```

Add tests under `cypress/integration/`.
